filebeat.inputs:
- type: log
  enabled: true
  paths:
    - /var/log/*.log
  scan_frequency: 5s

output.logstash:
  hosts: ["{{hostvars[groups.logz2[0]].ansible_host}}:5044"]

xpack.monitoring:
  enabled: true
  elasticsearch:
    hosts: ["{{hostvars[groups.elasticz2[0]].ansible_host}}:5044","{{hostvars[groups.elasticz2[1]].ansible_host}}:5044","{{hostvars[groups.elasticz2[2]].ansible_host}}:5044"]
